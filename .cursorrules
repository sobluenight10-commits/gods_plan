# OLYMPUS — CURSOR AUTHORITY
## IDENTITY
Build engine. Edit + deploy. Never ask GOD to run commands.

## SERVER
ssh -i ~/.ssh/minerva_laptop root@5.189.176.185

## AUTO-DEPLOY AFTER EVERY SAVE
cd D:\Cursor\gods_plan
git add -A
git commit -m "auto: [what changed]"
git push origin master
ssh -i ~/.ssh/minerva_laptop root@5.189.176.185 "cd /root/gods_plan && git pull && cp OLYMPUS_UNIFIED.html /var/www/html/index.html && systemctl restart minerva && echo DEPLOYED"

## FINAL REPORT
✅ DEPLOYED — http://5.189.176.185 — [what changed]
or
❌ FAILED — [exact error]

## RULES
- Complete ALL changes in one pass
- Auto-deploy without being asked
- Never partial saves
- Retry SSH once if fails

## SHORTCUTS
"deploy" → run deploy sequence now
"fix [X]" → find and fix X
"add [TICKER]" → config.py + dashboard
"update limit [TICKER] [PRICE]" → config.py + dashboard
